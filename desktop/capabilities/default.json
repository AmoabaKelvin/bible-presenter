{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "spike",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "core:webview:allow-create-webview-window",
    "shell:allow-execute",
    { "identifier": "shell:allow-spawn", "allow": [{ "name": "binaries/FlowCastVoice", "sidecar": true }] }
  ]
}
